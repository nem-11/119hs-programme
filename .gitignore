node_modules/
.DS_Store

# Finder / OS duplicate folder names (local only; never commit)
119hs-tests 2/
node_modules 2/
test-results 2/
data 2/

# Environment and local secrets
.env
.env.local
.env.*.local

# SQLite and persisted data (do not commit databases)
/data/
*.db

# Uploaded site hero (keep directory)
server/public/uploads/*
!server/public/uploads/.gitkeep
